//https://www.luogu.com.cn/problem/P1160
#include <iostream>
#include <vector>
#include <stdlib.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);
    long n,p,k,m,d;
    vector<long> arr{};
    cin >>n;
    vector<long*> a(n);
    for(long i=1;i<n;i++)
    {
        cin >>k >>p;
        if(p==0)
        {
            arr.insert(arr.begin())
        }
        else
        {
        }
    }
    cin >>m;
    for(long i=0;i<m;i++)
    {
        cin>>d;
    
    }
    return 0;
}